import { Stack, Typography } from '@mui/material';
import { FC } from 'react';

const App: FC = () => {
  return (
    <Stack>
      <Typography variant="h1">TerraHarbor Frontend</Typography>
    </Stack>
  );
};

export default App;
